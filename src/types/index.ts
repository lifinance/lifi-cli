export type {
  Chain,
  ChainType,
  Token,
  Route,
  Step,
  QuoteRequest,
  RoutesRequest,
  RoutesResponse,
} from '@lifi/types';

export interface GlobalOptions {
  json?: boolean;
  verbose?: boolean;
  noColor?: boolean;
}
